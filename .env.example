# HTTP 服务端口 (HTTPS 将使用 HTTP_PORT + 1)
HTTP_PORT=8080

# 目标服务主机 (默认: localhost)
# 在 host 网络模式下使用 localhost
# 在桥接网络模式下可能需要使用 host.docker.internal (macOS/Windows) 或宿主机IP (Linux)
TARGET_HOST=localhost

# 用户认证信息 (格式: username:bcrypt_hash)
# 使用以下命令生成密码哈希: node -e "console.log(require('bcryptjs').hashSync('your-password', 10))"
USER_CREDENTIALS=admin:$2a$10$rOOWkj8rOOWkj8rOOWkj8rOOWkj8u7wJ7wJ7wJ7wJ7wJ7wJ7wJ7wJ7w

# 会话密钥 (可选，留空将自动生成)
SESSION_SECRET=your-secret-key-here

# 日志级别
LOG_LEVEL=info

# Node.js 环境
NODE_ENV=production

# 网络模式标识 (用于日志记录)
NETWORK_MODE=host#
 MFA 密钥存储 (JSON 格式: {"username": "base32_secret"})
# 首次运行时留空，系统会引导用户设置 MFA
# 示例: MFA_SECRETS={"admin": "JBSWY3DPEHPK3PXP"}
MFA_SECRETS=# 时区
设置 (推荐使用 UTC 避免 MFA 时间同步问题)
TZ=UTC# 公开路径配置
 (无需认证即可代理的路径，JSON 数组格式)
# 示例: PUBLIC_PATHS=["/users/login","/api/auth","/public"]
PUBLIC_PATHS=